# Budget254 PAYE Admin Portal

## Run
```bash
npm install
cp .env.example .env
npm run dev
```

This is the Phase 8 foundation for dynamic payroll rule management.

The UI deliberately displays and edits data supplied by the backend. Kenyan figures must be seeded and verified in the database through governed rule versions, not embedded in React constants.
