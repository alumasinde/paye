# Phase 7 Android additions

New screens:
- Login
- Registration
- Saved calculation history

Tokens are stored with Expo SecureStore, not AsyncStorage.

Basic PAYE calculation remains anonymous. A logged-in user can save calculation snapshots and retrieve only their own history.
