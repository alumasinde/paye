APP_ENV=development
HTTP_PORT=8080
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=budget254_paye
DB_USER=budget254
DB_PASSWORD=change_me
JWT_SECRET=34487181dfb0030902e4b18ada182ece407f0321a1e284056ef54f9f37eaf1f3
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=720h
AUTH_MAX_FAILED_REQUESTS=10
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Allows Expo/Metro browser testing from localhost and private LAN IPs in development.
# Keep this false in production and list production origins explicitly.
CORS_ALLOW_PRIVATE_NETWORK_ORIGINS=true

# All of the below are optional for local dev - internal/config.Load()
# already defaults them sensibly. Uncomment to override.
# HTTP_MAX_REQUEST_BODY_BYTES=1048576
# HTTP_TRUST_PROXY_HEADERS=false
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_REQUESTS=60
# RATE_LIMIT_WINDOW=1m
# RATE_LIMIT_MAX_CLIENTS=10000
# LOG_LEVEL=INFO
# See .env.production.example for the full production reference.
